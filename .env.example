# Copy to `.env.local` and fill in. Env files are gitignored.

# Gemini key (optional; used by the AI Studio tooling).
GEMINI_API_KEY=your_gemini_key_here

# Note: the energy dashboard pipeline (`npm run build:energy`) needs NO token —
# it reads OMIE's public bids + official unit→technology registry directly.
